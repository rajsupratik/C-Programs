#include<stdio.h>
int bubbleSort(int arr[],int size) {
    for (int i = 0; i < size; i++) {
        for (int j = 0; j < (size-i-1); j++) {
            if (arr[j] > arr[j+1]) {
            int temp = arr[j];
            arr[j] = arr[j+1];
            arr[j+1] = temp;
            }
        }
    }    
}

int main(){
    int elements[] = {8, 6, 9, 2, 3};
    int size = sizeof(elements) / sizeof(elements[0]);

    bubbleSort(elements, size);

    printf("Sorted Arrays: ");
    for (int i = 0; i < size; i++) {
        printf("%d", elements[i]);
    }
    printf("\n");

    return 0;
}
